server {
   listen       3500;
   server_name  panaceum.tomesprout.com/api;

   location / {
       root   /usr/share/nginx/html;
       index  index.html;
       try_files $uri $uri/ /index.html;
   }
}